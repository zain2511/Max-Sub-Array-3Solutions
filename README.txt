Three different ways to solve max-sub-array problem.

1) Brute Force (easiest) but solves in O(n^2) time
2) Recursive (uses divide and conquer) solves in O(nlogn)
3) Iterative (most optimal approach) solves in O(n)